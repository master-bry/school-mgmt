import ParentsManager from '../ParentsManager'
const AHParents = () => (
  <ParentsManager
    apiPrefix="/api/assistant-head"
    title="Parents Directory"
    subtitle="View all parents extracted from student records — no duplicates"
  />
)
export default AHParents
